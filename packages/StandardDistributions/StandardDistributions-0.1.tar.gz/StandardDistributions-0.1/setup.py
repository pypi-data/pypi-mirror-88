from setuptools import setup

setup(name='StandardDistributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['StandardDistributions'],
      zip_safe=False)
