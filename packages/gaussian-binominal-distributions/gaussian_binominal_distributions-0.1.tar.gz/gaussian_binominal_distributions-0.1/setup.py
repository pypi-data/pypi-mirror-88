from setuptools import setup

setup(name='gaussian_binominal_distributions',
      version='0.1',
      description='Gaussian & Binominal distributions',
      packages=['l0ltestpypi'],
      zip_safe=False)
