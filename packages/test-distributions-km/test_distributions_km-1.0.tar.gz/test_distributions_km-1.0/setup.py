from setuptools import setup

setup(name='test_distributions_km',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['test_distributions_km'],
      author='Kengo Matsumura',
      zip_safe=False)