[metadata]
name = {{ project_name }}
version = attr: {{ project_name }}.__version__

[options]
package = find:
package_dir = = src
include_package_data = true
python_requires = >= 3.6

[options.packages.find]
where = src

[tool:pytest]
testpaths = tests