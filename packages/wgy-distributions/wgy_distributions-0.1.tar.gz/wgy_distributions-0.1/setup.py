from setuptools import setup

setup(name='wgy_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['wgy_distributions'],
      zip_safe=False)
