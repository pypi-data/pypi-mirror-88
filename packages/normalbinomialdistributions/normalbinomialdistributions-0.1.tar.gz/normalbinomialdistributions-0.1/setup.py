from setuptools import setup

setup(name='normalbinomialdistributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['normalbinomialdistributions'],
      zip_safe=False)
