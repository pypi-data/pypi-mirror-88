import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('graal',));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('graal', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('graal', [os.path.dirname(p)])));m = m or sys.modules.setdefault('graal', types.ModuleType('graal'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p)
import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('graal',));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('graal', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('graal', [os.path.dirname(p)])));m = m or sys.modules.setdefault('graal', types.ModuleType('graal'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p)
import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('graal', 'backends'));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('graal.backends', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('graal.backends', [os.path.dirname(p)])));m = m or sys.modules.setdefault('graal.backends', types.ModuleType('graal.backends'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p);m and setattr(sys.modules['graal'], 'backends', m)
