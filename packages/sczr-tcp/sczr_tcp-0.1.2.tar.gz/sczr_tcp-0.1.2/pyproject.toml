[tool.poetry]
name = "sczr_tcp"
version = "0.1.2"
description = ""
authors = ["Michał Rokita <mrokita@mrokita.pl>"]

[tool.poetry.scripts]
sczr-tcp = "sczr_tcp.cli:main"

[tool.poetry.dependencies]
python = "^3.9"
pydantic = "^1.7.3"
orjson = "^3.4.6"
pygame = "^2.0.0"
loguru = "^0.5.3"
typer = "^0.3.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
